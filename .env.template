# =============================================================================
# FOUNDRY AGENT ACCELERATOR - Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.template .env
#
# NEVER commit your .env file to source control!
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED SETTINGS
# -----------------------------------------------------------------------------
# These must be set for the application to work

# Your Azure AI Foundry project endpoint
# Format: https://<resource-name>.services.ai.azure.com/api/projects/<project-name>
AZURE_EXISTING_AIPROJECT_ENDPOINT=

# The deployment name of your chat model (e.g., gpt-4o, gpt-4o-mini)
AZURE_AI_CHAT_DEPLOYMENT_NAME=

# The name for your agent in Azure AI Foundry
# This agent will be created/updated on startup and visible in the Foundry portal
# Use a descriptive name like "customer-support-agent" or "sales-assistant"
AZURE_AI_AGENT_NAME=

# -----------------------------------------------------------------------------
# OPTIONAL SETTINGS
# -----------------------------------------------------------------------------
# These have sensible defaults but can be customized

# Server port (default: 8000)
# PORT=8000

# Logging level (default: INFO)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOGGING_LEVEL=INFO

# Azure tenant ID (only needed if you have multiple Azure tenants)
# AZURE_TENANT_ID=

# -----------------------------------------------------------------------------
# TOOL-SPECIFIC SETTINGS
# -----------------------------------------------------------------------------
# Optional settings for agent tools. Most tools are configured in agent.yaml,
# but some may use environment variables as an alternative.

# Image Generation deployment name (only needed if not set in agent.yaml)
# Deploy gpt-image-1 in your Foundry project and set the deployment name here
# IMAGE_GENERATION_DEPLOYMENT_NAME=gpt-image-1
