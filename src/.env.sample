# =============================================================================
# FOUNDRY AGENT ACCELERATOR - Environment Configuration
# =============================================================================
#
# This file contains the environment variables needed to run the application.
# Copy this file to '.env' and fill in your values.
#
# HOW TO GET THESE VALUES:
# 1. Deploy to Azure using 'azd up', OR
# 2. Find them in your Azure AI Foundry project settings
#
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED VARIABLES - The app won't work without these
# -----------------------------------------------------------------------------

# Your Azure AI Foundry project endpoint
# Format: https://<your-account>.services.ai.azure.com/api/projects/<project-name>
# Find this in: Azure AI Foundry > Your Project > Overview > Project endpoint
AZURE_EXISTING_AIPROJECT_ENDPOINT=""

# The name for your agent in Azure AI Foundry
# This is how your agent appears in the Foundry portal
# Use lowercase with hyphens (e.g., "customer-support-agent")
AZURE_AI_AGENT_NAME="my-agent"


# -----------------------------------------------------------------------------
# CONFIGURATION SOURCE - Where is your agent configured?
# -----------------------------------------------------------------------------
#
# Choose where the agent configuration comes from:
#
# "local" (default)
#   - Agent is configured via prompts/system.txt and agent.yaml
#   - Edit these files to change agent behavior and tools
#   - Good for development, testing, and GitOps workflows
#   - Requires AZURE_AI_CHAT_DEPLOYMENT_NAME to be set
#
# "portal"
#   - Agent is configured entirely in Azure AI Foundry portal
#   - Local files (prompts/system.txt, agent.yaml) are IGNORED
#   - Changes in portal take effect immediately (no redeploy)
#   - Good for production when business users manage the agent
#
AGENT_CONFIG_SOURCE=local


# -----------------------------------------------------------------------------
# MODEL CONFIGURATION (Required for local mode only)
# -----------------------------------------------------------------------------

# The name of your deployed chat model
# Examples: "gpt-4o-mini", "gpt-4o", "gpt-35-turbo"
# Find this in: Azure AI Foundry > Your Project > Deployments
# Note: Not required in portal mode (model is set in portal)
AZURE_AI_CHAT_DEPLOYMENT_NAME="gpt-4o-mini"


# -----------------------------------------------------------------------------
# OPTIONAL VARIABLES - For additional features
# -----------------------------------------------------------------------------

# Azure Tenant ID (usually auto-detected, only set if you have multiple tenants)
# AZURE_TENANT_ID=""

# Basic authentication for the web app (optional, leave empty to disable)
# WEB_APP_USERNAME=""
# WEB_APP_PASSWORD=""

# Optional log file path (logs go to console by default)
# APP_LOG_FILE=""
