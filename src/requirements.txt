# =============================================================================
# FOUNDRY AGENT ACCELERATOR - Python Dependencies
# =============================================================================
# 
# This file lists all Python packages the application needs to run.
# When you run 'pip install -r requirements.txt', these packages are installed.
#
# REQUIRED PACKAGES (Core functionality):
# - fastapi: Web framework for handling API requests
# - uvicorn/gunicorn: Web servers to run the application
# - azure-identity: Authentication with Azure services
# - azure-ai-inference: Communicate with Azure AI models
# - azure-ai-projects: Connect to Azure AI Foundry projects
#
# OPTIONAL PACKAGES (RAG feature):
# - azure-search-documents: Required only if using RAG document search
#
# =============================================================================

# -----------------------------------------------------------------------------
# WEB FRAMEWORK - FastAPI and servers
# -----------------------------------------------------------------------------
fastapi>=0.121.2        # Web framework for building APIs
uvicorn[standard]==0.29.0  # Development server (runs the app locally)
gunicorn==23.0.0        # Production server (runs the app in Azure)
jinja2                  # Template engine for HTML pages

# -----------------------------------------------------------------------------
# CONFIGURATION
# -----------------------------------------------------------------------------
pyyaml                  # For parsing agent.yaml configuration
python-dotenv           # For loading .env files

# -----------------------------------------------------------------------------
# AZURE AUTHENTICATION
# -----------------------------------------------------------------------------
azure-identity==1.19.0  # Handles login/authentication with Azure

# -----------------------------------------------------------------------------
# AZURE AI - Core packages for AI functionality
# -----------------------------------------------------------------------------
# NOTE: Using pre-release version for Foundry Agents support (create_version API)
azure-ai-projects>=2.0.0b1   # Connect to Azure AI Foundry projects with Agents API
azure-core==1.34.0           # Base Azure SDK functionality
openai                       # OpenAI client for agent responses API

# -----------------------------------------------------------------------------
# NETWORKING
# -----------------------------------------------------------------------------
aiohttp==3.13.1         # Async HTTP client (used by Azure SDK)

# -----------------------------------------------------------------------------
# [OPTIONAL] RAG SUPPORT - Azure AI Search
# -----------------------------------------------------------------------------
# These packages are only needed if you want to use the RAG feature
# (search through your documents to answer questions)
azure-search-documents  # Search and index documents in Azure AI Search

# -----------------------------------------------------------------------------
# SECURITY FIXES
# -----------------------------------------------------------------------------
starlette>=0.47.2       # Security fix for CVE-2025-54121
setuptools==80.9.0      # Build tools